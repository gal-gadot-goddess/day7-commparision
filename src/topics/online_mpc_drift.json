{
  "title": "Online MPC Drift",
  "tagline": "Compare three linear-time plant models and one quadratic baseline for tracking control under measurement noise.",
  "xAxisLabel": "Horizon Length n",
  "yAxisLabel": "Tracking Error Score (0-100+)",
  "curves": [
    {
      "id": "1",
      "label": "Kalman Linear Update",
      "color": "#00C2FF",
      "formula": "n => 2 + 12 * Math.log2(n+1) + 0.08*n",
      "code": "for (let t=1;t<=n;t++){ err += 12*Math.log2(t+1)-12*Math.log2(t); err += 0.08; } score=2+err;",
      "name": "Kalman Linear Update"
    },
    {
      "id": "2",
      "label": "RLS Exponential Forgetting",
      "color": "#7CFF00",
      "formula": "n => 3 + 9 * Math.log2(n+1) + 0.05*n + 0.002*n*Math.log2(n+1)",
      "code": "let s=0; for(let t=1;t<=n;t++){ s += 9*(Math.log2(t+1)-Math.log2(t)); s += 0.05; s += 0.002*(t*Math.log2(t+1)- (t-1)*Math.log2(t)); } score=3+s;",
      "name": "RLS Exponential Forgetting"
    },
    {
      "id": "3",
      "label": "Subspace System ID",
      "color": "#FFB300",
      "formula": "n => 5 + 18 * Math.pow(n,0.55) + 0.03*n",
      "code": "let s=0; for(let t=1;t<=n;t++){ s += 18*(Math.pow(t,0.55)-Math.pow(t-1,0.55)); s += 0.03; } score=5+s;",
      "name": "Subspace System ID"
    },
    {
      "id": "4",
      "label": "Direct Least Squares Baseline",
      "color": "#FF2D55",
      "formula": "n => 6 + 0.12*n*Math.log2(n+1) + 0.0007*n*n",
      "code": "let s=0; for(let t=1;t<=n;t++){ s += 0.12*(t*Math.log2(t+1)-(t-1)*Math.log2(t)); s += 0.0007*(t*t-(t-1)*(t-1)); } score=6+s;",
      "name": "Direct Least Squares Baseline"
    }
  ]
}