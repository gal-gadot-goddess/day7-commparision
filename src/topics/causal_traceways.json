{
  "title": "Causal Traceways",
  "tagline": "Compare three causality models and two propagation strategies for distributed event visualization.",
  "xAxisLabel": "Events (n)",
  "yAxisLabel": "Trace Construction Cost (0-100+)",
  "curves": [
    {
      "id": "1",
      "label": "Lamport Timestamps",
      "color": "#00C2FF",
      "formula": "n => 0.02 * n + 0.00005 * n * Math.log(n + 1)",
      "code": "for (let i=0;i<n;i++){ ts[i]=max(ts[i],recvTs[i])+1; } buildEdgesFromPartialOrder(ts);",
      "name": "Lamport Timestamps"
    },
    {
      "id": "2",
      "label": "Vector Clocks (Full)",
      "color": "#FFB000",
      "formula": "n => 0.004 * n * n + 0.03 * n",
      "code": "let V = new Array(n).fill(0).map(()=>new Int32Array(p)); for (let i=0;i<n;i++){ merge(V[i], V[from]); V[i][self]++; } deriveHB(V);",
      "name": "Vector Clocks (Full)"
    },
    {
      "id": "3",
      "label": "Interval VC (Per-Actor)",
      "color": "#2ECC71",
      "formula": "n => 0.06 * n + 0.02 * n * Math.log(n + 1)",
      "code": "for (let i=0;i<n;i++){ intervals[i]=updateActorIntervals(actor=i%p, seq=++k); } buildHBFromIntervals(intervals);",
      "name": "Interval VC (Per-Actor)"
    },
    {
      "id": "4",
      "label": "DAG Watermarks (Greedy)",
      "color": "#8E44AD",
      "formula": "n => 0.08 * n * Math.log(n + 1) + 0.015 * n",
      "code": "for (let e=0;e<n;e++){ insertIntoDAG(e); while (canAdvanceWatermark()) pruneOldEdges(); } renderReachability(DAG);",
      "name": "DAG Watermarks (Greedy)"
    },
    {
      "id": "5",
      "label": "Hybrid Sampling HB",
      "color": "#FF4D6D",
      "formula": "n => 0.2 * Math.sqrt(n) + 0.04 * n + 0.01 * n * Math.log(n + 1)",
      "code": "let sampleRate = 1/Math.max(1, Math.sqrt(n)); for (let i=0;i<n;i++){ if (Math.random()<sampleRate) keep[i]=true; } estimateHB(keep); renderApproxCausality(estimate);",
      "name": "Hybrid Sampling HB"
    }
  ]
}