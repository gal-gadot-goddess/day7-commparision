{
  "title": "Sparse Pivoting",
  "tagline": "Compare sparse linear solver strategies by fill-in growth and solve cost.",
  "xAxisLabel": "Nonzeros (n)",
  "yAxisLabel": "Relative Cost (0-100+)",
  "curves": [
    {
      "id": "1",
      "label": "Static ILU(0)",
      "color": "#1E88E5",
      "formula": "n => 0.02*n + 0.00000008*n*n",
      "code": "for (k=0;k<nz;k++){ apply_row(k); /* fixed drop */ } solve_triangular();",
      "name": "Static ILU(0)"
    },
    {
      "id": "2",
      "label": "Threshold ILUT",
      "color": "#43A047",
      "formula": "n => 0.06*n* Math.log(n) + 0.0000003*n",
      "code": "for (k=0;k<nz;k++){ residual = compute_residual(k); keep = residual>tau; build_factors(keep); } triangular_solve();",
      "name": "Threshold ILUT"
    },
    {
      "id": "3",
      "label": "AMD Reordering + SpLU",
      "color": "#FB8C00",
      "formula": "n => 0.35*Math.pow(n,0.9) + 0.02*n*Math.log(n)",
      "code": "perm=amd(A); LU=spilu(A,perm); forward_sub(L); backward_sub(U);",
      "name": "AMD Reordering + SpLU"
    },
    {
      "id": "4",
      "label": "Nested Dissection",
      "color": "#8E24AA",
      "formula": "n => 0.12*Math.pow(n,1.5) + 0.8*n*Math.log(n)",
      "code": "tree = bisect_graph(A); for (sep in separators){ factorize(sep); eliminate_subdomains(); } assemble_solve();",
      "name": "Nested Dissection"
    },
    {
      "id": "5",
      "label": "Multigrid V-Cycle (Idealized)",
      "color": "#00ACC1",
      "formula": "n => 0.03*n + 0.2*Math.log(n) * n/1 + 0.000001*n",
      "code": "for(v=0; v<V; v++){ relax_smooth(A,x,b); x=restrict(x); if(level>0) recurse(); x=prolong(x); correct(); }",
      "name": "Multigrid V-Cycle (Idealized)"
    }
  ]
}