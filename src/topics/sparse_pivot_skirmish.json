{
  "title": "Sparse Pivot Skirmish",
  "tagline": "Compare four sparse pivoting strategies by fill-in growth during Gaussian elimination.",
  "xAxisLabel": "Matrix Dimension n (sparsity fixed)",
  "yAxisLabel": "Estimated Fill-In Cost (0-100+)",
  "curves": [
    {
      "id": "1",
      "label": "No Pivoting",
      "color": "#2E86AB",
      "formula": "n => 0.02 * n * n",
      "code": "for(let i=0;i<n;i++){ // assume row operations; fill-in tends to explode\n  for(let j=i+1;j<n;j++){\n    if(a[i][i]!==0){\n      // update row j columns (conceptual)\n    }\n  }\n}",
      "name": "No Pivoting"
    },
    {
      "id": "2",
      "label": "Partial Pivoting (Column Max)",
      "color": "#F18F01",
      "formula": "n => 0.015 * n*n + 0.08 * n * Math.log(n+1)",
      "code": "for(let k=0;k<n;k++){\n  // select pivot row r with max |a[r][k]| among r>=k\n  let r=k;\n  for(let i=k+1;i<n;i++) if(Math.abs(a[i][k])>Math.abs(a[r][k])) r=i;\n  // swap rows k and r\n  for(let i=k+1;i<n;i++){\n    // eliminate with chosen pivot; fill-in grows slower than no pivot\n  }\n}",
      "name": "Partial Pivoting (Column Max)"
    },
    {
      "id": "3",
      "label": "Minimum Degree Pivoting",
      "color": "#6C5CE7",
      "formula": "n => 0.03 * n * Math.log(n+1) + 0.001 * n*n",
      "code": "const deg=new Array(n).fill(0);\nfor(let k=0;k<n;k++){\n  // pick pivot among remaining nodes with minimal estimated degree\n  let p=k;\n  for(let i=k;i<n;i++) if(deg[i]<deg[p]) p=i;\n  // eliminate p; update degrees of neighbors\n  for(let t=0;t<n;t++) if(/* neighbor relation */ false){\n    // deg[t] = updated degree after fill-in\n  }\n}",
      "name": "Minimum Degree Pivoting"
    },
    {
      "id": "4",
      "label": "Nested Dissection Pivoting",
      "color": "#00B894",
      "formula": "n => 0.02 * n * (Math.log(n+1) ** 2)",
      "code": "function solve(nodes){\n  if(nodes.length<=1) return;\n  // choose separator S that splits graph into A and B\n  // eliminate A and B recursively, then handle S\n  solve(A);\n  solve(B);\n  // elimination on separator adds comparatively smaller fill-in\n}\nsolve([...Array(n).keys()]);",
      "name": "Nested Dissection Pivoting"
    }
  ]
}