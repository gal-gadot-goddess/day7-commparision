{
  "title": "RegEx Engine Duel",
  "tagline": "Compare backtracking, Thompson NFA, DFA compilation, and lazy derivatives for worst-case-safe matching.",
  "xAxisLabel": "Input length n (with fixed regex size m)",
  "yAxisLabel": "Estimated match cost (ops)",
  "curves": [
    {
      "id": "1",
      "label": "Backtracking NFA",
      "color": "#FF4D4D",
      "formula": "n => 0.02 * Math.pow(2, n) + 0.5 * n",
      "code": "function backtrack(n){ for(let i=0;i<n;i++){ /* explore branches recursively */ } /* exponential blow-up on crafted patterns */ }",
      "name": "Backtracking NFA"
    },
    {
      "id": "2",
      "label": "Thompson NFA",
      "color": "#00C2FF",
      "formula": "n => 2.5 * n * 10 + 30",
      "code": "function thompson(n){ let active=new Set(); for(let i=0;i<n;i++){ /* advance active states */ } return active.size; }",
      "name": "Thompson NFA"
    },
    {
      "id": "3",
      "label": "DFA Compilation",
      "color": "#2ECC71",
      "formula": "n => 0.8 * n + 250",
      "code": "function dfa(n){ /* precompute transition table once */ for(let i=0;i<n;i++){ /* one transition */ } }",
      "name": "DFA Compilation"
    },
    {
      "id": "4",
      "label": "Brzozowski Derivatives (Lazy)",
      "color": "#F5A623",
      "formula": "n => 6 * n * Math.log(n + 1) + 120",
      "code": "function derivatives(n){ let state='r0'; for(let i=0;i<n;i++){ /* compute next derivative lazily with memo */ } }",
      "name": "Brzozowski Derivatives (Lazy)"
    },
    {
      "id": "5",
      "label": "Partial DFA with Pruning",
      "color": "#8E44AD",
      "formula": "n => 18 * n + 80 * Math.sqrt(n) + 60",
      "code": "function partialDfa(n){ /* build only reachable states; prune low-probability branches */ for(let i=0;i<n;i++){ /* transitions */ } }",
      "name": "Partial DFA with Pruning"
    }
  ]
}