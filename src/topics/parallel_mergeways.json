{
  "title": "Parallel Mergeways",
  "tagline": "Compare four merge strategies for parallel external sorting under memory pressure.",
  "xAxisLabel": "Input size n (items)",
  "yAxisLabel": "Estimated time (0–100+ scale)",
  "curves": [
    {
      "id": "1",
      "label": "k-Way Heap Merge",
      "color": "#4B7BEC",
      "formula": "n => 0.02 * n * (Math.log2(n + 1))",
      "code": "function kWayHeapMerge(runs){ /* push first element of each run into min-heap */ /* repeat: pop min, advance that run, push next */ }",
      "name": "k-Way Heap Merge"
    },
    {
      "id": "2",
      "label": "Loser Tree Merge",
      "color": "#20C997",
      "formula": "n => 0.018 * n * (Math.log2(n + 1))",
      "code": "function loserTreeMerge(runs){ /* build loser tree over run heads */ /* each extraction updates O(log k) via tree comparisons */ }",
      "name": "Loser Tree Merge"
    },
    {
      "id": "3",
      "label": "Divide-Merge Network",
      "color": "#F7B801",
      "formula": "n => 0.004 * n * n / (Math.log2(n + 1))",
      "code": "function mergeNetwork(runs){ /* iteratively pairwise merge in parallel stages */ /* stage count grows with log n; contention rises with run fan-in */ }",
      "name": "Divide-Merge Network"
    },
    {
      "id": "4",
      "label": "Naive Sequential Stitch",
      "color": "#FD3A6A",
      "formula": "n => 0.0009 * n * n",
      "code": "function sequentialStitch(runs){ /* merge run1+run2 -> temp; temp+run3 -> temp2; ... */ /* fully serializes merge steps */ }",
      "name": "Naive Sequential Stitch"
    }
  ]
}