{
  "title": "Hysteresis Caches",
  "tagline": "Compare how different cache admission policies trade hit-rate versus overhead as workload size grows.",
  "xAxisLabel": "Working Set Size (n)",
  "yAxisLabel": "Relative Cost (0-100 scale)",
  "curves": [
    {
      "id": "1",
      "label": "LRU Admission",
      "color": "#2E86AB",
      "formula": "n => 4 + 0.9 * Math.log(n + 1) + 0.01 * n",
      "code": "if(cache.has(x)){cache.touch(x)}else{cache.insert(x); cache.evictLRU()}",
      "name": "LRU Admission"
    },
    {
      "id": "2",
      "label": "TinyLFU Admission",
      "color": "#F18F01",
      "formula": "n => 6 + 1.2 * Math.log(n + 1) + 0.0008 * n * n",
      "code": "if(probe(x)){accept();} else {score=x.count; victimScore=sample(victim).count; if(score>victimScore) insertAndDemote(victim);}",
      "name": "TinyLFU Admission"
    },
    {
      "id": "3",
      "label": "CLOCK-Pro Cycles",
      "color": "#7D3C98",
      "formula": "n => 8 + 0.6 * n + 0.002 * n * Math.log(n + 1)",
      "code": "while(hand.scan()){ if(refbit==0 && state==hot){demoteToWarm();} else if(state==warm && refbit==0){ghostInsert();} if(state==ghost && refbit==0){evict(); break;} hand.advance(); }",
      "name": "CLOCK-Pro Cycles"
    },
    {
      "id": "4",
      "label": "RRR Rate-Limited",
      "color": "#2ECC71",
      "formula": "n => 5 + 0.03 * n * n",
      "code": "bucket=hash(x)%B; if(tokens[bucket]>0){tokens[bucket]--; accept(x);} else {reject(x);} periodically refill tokens per interval",
      "name": "RRR Rate-Limited"
    },
    {
      "id": "5",
      "label": "Adaptive Hysteresis",
      "color": "#E74C3C",
      "formula": "n => 3 + 0.8 * Math.log(n + 1) + 0.0015 * n * n / (1 + 0.05 * Math.sqrt(n))",
      "code": "if(hystState==warmToHot){if(score(x)>hi && recentMissRateLow()) promote(x); else keep(x);} else {if(score(x)<lo || recentMissRateHigh()) demoteOrReject(x);} update thresholds via exponential smoothing",
      "name": "Adaptive Hysteresis"
    }
  ]
}